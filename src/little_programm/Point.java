package little_programm;

public class Point {
    int x;
    int y;

    Point(int x, int y){
        this.x = x;
        this.y = y;
    }
    public void print(){
        System.out.println("Точка: (" + x + ", " + y + ")");
    }

    public void move(int dx, int dy){
        x += dx;
        y += dy;
    }

    public double distanceTo(Point other){
        double dx = other.x - this.x;
        double dy = other.y - this.y;

        return Math.sqrt(dx * dx + dy * dy);
    }

    public static void main(String[] args) {
        Point point = new Point(3,2);
        point.print();
        point.move(4,5);
        point.print();
        Point point1 = new Point(0, 0);
        Point point2 = new Point(2,3);
        double distanceTo = point1.distanceTo(point2);
        System.out.println(distanceTo);

    }

}
//Используй this.x и this.y — это координаты текущей точки.
//Используй other.x и other.y — это координаты другой точки, переданной в метод.
//Посчитай разницу по x и по y.
//Возведи их в квадрат.
//Сложи и возьми квадратный корень (в Java это Math.sqrt(...)).
//Верни результат через return.
//        ✏️ Реализуй тело метода. Как закончишь — покажи!
//Шаг 6: Протестируем наш класс
//Теперь, когда у нас есть все необходимые методы, давай протестируем их.
//Создадим точку с определёнными координатами.
//Выведем её.
//Переместим точку.
//Снова выведем её, чтобы увидеть изменения.
//✏️ Напиши в методе main следующий код:
//Создай объект Point с координатами, например, (0, 0).
//Вызови метод print для вывода координат.
//Вызови метод move, чтобы переместить точку, например, на (3, 4).
//Снова вызови метод print, чтобы увидеть новые координаты.
//После этого покажи, что получилось!
///////
//Напиши метод move:
//Он должен принимать два параметра: dx (сдвиг по оси x) и dy (сдвиг по оси y).
//Метод должен прибавлять dx к текущему значению x и прибавлять dy к текущему значению y.
//🔹 Шаг 3: Создание метода для вывода информации о точке
//Теперь добавим метод print, который будет выводить информацию о точке. Мы будем просто выводить значения x и y.
//
//        ✏️ Напиши метод print:
//
//метод без параметров
//
//выводи на экран координаты точки: x и y
//
//🔹 Шаг 2: Добавление конструктора
//Нам нужно добавить конструктор, который будет инициализировать координаты точки.
//В конструкторе ты будешь принимать значения для x и y, и устанавливать их в соответствующие поля.
//
//✏️ Напиши:
//
//конструктор Point(int x, int y), который будет присваивать значения этим полям.
//        Как сделаешь, покажи!
//Шаг 1: создай новый класс Point
//В этом классе будет 2 поля: x и y (координаты точки).
//
//        ✏️ Напиши:
//
//объявление класса
//
//два поля x и y типа int


